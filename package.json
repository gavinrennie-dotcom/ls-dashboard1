{
  "name": "bloqit-ls-dashboard",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:cloudflare": "npm run build && wrangler pages dev dist",
    "deploy:alerts": "wrangler deploy --config cloudflare/wrangler.jsonc"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "vite": "^8.1.4",
    "wrangler": "^4.0.0"
  }
}
